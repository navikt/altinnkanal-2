<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/static/css/bootstrap.min.css">
    <link rel="stylesheet" href="/static/css/app.css">
    <title>Altinnkanal Configuration Page</title>
</head>
<body>
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <a href="/configuration" class="navbar-brand">Altinnkanal Adminkonsoll</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse">

                <ul class="nav navbar-nav navbar-left">
                    <li>
                        <a href="/configuration/new">Ny videresending</a>
                    </li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    {{# ident }}
                        <li class="navbar-text">
                            Logget inn som: {{ ident }}
                        </li>
                        <li class="navbar-btn">
                            <form action="/configuration/logout" method="post">
                                <button type="submit" class="btn btn-link" value="Logg ut">Logg ut</button>
                                <input type="hidden" name="{{_csrf.parameterName }}" value="{{ _csrf.token }}">
                            </form>
                        </li>
                    {{/ ident }}
                    {{^ ident }}
                        <li>
                            <a href="/configuration/login">Logg inn</a>
                        </li>
                    {{/ ident }}
                </ul>
            </div>
        </div>
    </nav>
    <div class="container-fluid">
        <div class="row">
            {{{ layout.body }}}
        </div>
    </div>
</body>
</html>
